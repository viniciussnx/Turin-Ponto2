---
category: marca-e-icones
---
Ícones de traço em SVG (24×24, traço 1,8). `name`: home, clock, mirror, inbox, user, pin, face, check, swap, doc, calendar, alert, bell, moon, shield, coffee, bus, chevron-down, chevron-up, chevron-right, chevron-left, plus, camera, lock, logout. `color` é obrigatório (use tokens: `c.muted`, `c.brandInk`, `#FFFFFF` no cabeçalho); `size` padrão 22.

```tsx
<Icon name="clock" color={c.brandInk} size={20} />
```
