---
category: navegacao
---
Filtros em pílula num carrossel horizontal. Ativo: fundo `brandAction`, texto `onBrand`; inativo: `surface` com borda `line`. Altura mínima 44 pt.

```tsx
<ChipFilters options={['Todos', 'Pendências', 'Extras']} value={filtro} onChange={setFiltro} />
```
