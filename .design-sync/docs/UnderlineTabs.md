---
category: navegacao
---
Abas sublinhadas de largura igual; a ativa tem texto `text` e sublinhado `brandAction` de 2 pt. Usado na tela de Solicitações logo abaixo do cabeçalho, sobre `surface`.

```tsx
<UnderlineTabs options={['Todas', 'Em análise', 'Aprovadas', 'Recusadas']} value={aba} onChange={setAba} />
```
