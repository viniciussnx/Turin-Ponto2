---
category: formularios
---
Caixa de seleção com rótulo ("Manter conectado"). A linha inteira é o alvo de toque (44 pt). Marcada: fundo `brandAction` com visto `onBrand`.

```tsx
<Checkbox checked={manter} onChange={setManter} label="Manter conectado" />
```
