---
category: layout
---
Rótulo de seção: Inter SemiBold 13, cor `text2`, sentence case (nunca caixa alta). Vai acima de um grupo de cards ou dentro de um `Card` como título. `children` é texto.

```tsx
<SectionLabel>Preferências</SectionLabel>
```
