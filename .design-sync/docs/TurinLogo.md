---
category: marca-e-icones
---
Marca da Turin Transportes (traço vazado, tingido por `color`). Branca (`#FFFFFF`) sobre o degradê do cabeçalho/login; `brandInk` sobre fundo claro. `width` padrão 148 (a altura é 28% da largura).

```tsx
<TurinLogo color="#FFFFFF" width={152} />
```
