---
category: feedback
---
Estado vazio centralizado: ícone num círculo `surface2` de 60 pt, título 16 e detalhe `muted` até 280 pt de largura.

```tsx
<EmptyState icon="inbox" title="Nenhuma solicitação"
  detail="Quando precisar corrigir uma marcação ou justificar uma falta, abra um pedido pelo botão +." />
```
